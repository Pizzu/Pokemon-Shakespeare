export const getPokemonDescription = (req, res, next) => {
  // validate input
  // fetch description
  // create response object
  res.json({name: "Pokemon Name", description: "Pokemon Description"})
}